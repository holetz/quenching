---
slug: decide-plan-quick-skill
title: Decide whether quenching-specs-plan-quick is still needed
verification: per-section
---

# Decide whether quenching-specs-plan-quick is still needed

## Overview

- none — only `## Problem` is filled; nothing else exists yet to connect.

## Problem

Open decision from the refine-and-execute-specs-flow plan — re-evaluate the express lane after two weeks of using the chained path

_(v1 backlog task — tags: ['specs', 'skills', 'taxonomy'])_

An express-lane skill running propose → branch → apply → archive under one confirmation was
rejected **for now** in favour of chaining (`from-claude` offers to chain into apply, `apply` offers
to chain into archive at 100%), because chaining delivers the same path with no 29th always-on
description. The plan's design says the call is decided "by using the chained path on real small
work for two weeks, not by argument" — so revisit from about 2026-08-08. If the friction survives,
the skill is back on the table; if it does not, close this and record the answer.
