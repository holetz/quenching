# `knowledge/automation/` — understanding about this plugin's own tooling

* [skills.py lint exits 0 with warning-severity findings still present](skills-lint-warning-exit-code.md) — the exit code alone never proves a warning-severity finding closed, and a single-file invocation re-roots the reported command path.
