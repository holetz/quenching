# Brand assets

Place the binding lockup and licensed font files here, then cite each path from
`/.knowledge/standards/design/brand.md` or `/.design/tokens.json`. `cq design doctor` reports an
uncited file as an orphan. Montserrat is named by the starter tokens; if it is distributed with
the project, include its OFL license beside the font files.
