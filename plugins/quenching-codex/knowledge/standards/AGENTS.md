# AGENTS.md — /docs/standards/

Current/active **contracts and conventions** of this repo — one standard per file,
organized in subject subfolders. This is the detail the root pointer cites on demand.

- **Where each standard lives:** [index.md](index.md) — the layer listing (its
  "Current docs" tables are DERIVED from disk; do not hand-edit the `GENERATED` zone).
- **To create / edit / move / rename a standard here** (keeping the listing in sync):
  use the `quenching` skills — `quenching:knowledge:add` to add one, `quenching:knowledge:align`
  to migrate/normalize the layer.
- **Unfamiliar term?** Resolve it in the glossary first:
  [/docs/glossary.md](/docs/glossary.md) (`grep -i '<term>'`) — the repo's
  A–Z lookup, one entry per term with a link to its full doc when one exists.
- **Boundary:** the contract for how WE build lives here — a proven rule is `authority: current`,
  an agreed-but-unproven one `authority: background` (no separate decisions home). Direction →
  `vision/`, external fact → `external/`, our data → `catalog/`.
- **OKF:** every standard carries `type: standard` + a derived `resource:` (the repo
  scope it governs, from the doc's `file:line` anchors — never invented).

<!-- Auto-loaded by Codex when working under /docs/standards/. Keep it a thin
     pointer (nearest-file navigation), never a copy of the standards. Installed by the
     quenching plugin; this file is a harness pointer, not an OKF concept. -->
