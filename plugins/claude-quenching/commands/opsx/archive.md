---
description: Archive a completed OpenSpec change, then offer the OKF distillation pass
argument-hint: [change-name]
---

Use the Skill tool to invoke `claude-quenching:openspec-archive-change`, passing
`$ARGUMENTS` (optionally the change name) as the skill's input.
