---
description: Implement tasks from an OpenSpec change
argument-hint: [change-name]
---

Use the Skill tool to invoke `claude-quenching:openspec-apply-change`, passing `$ARGUMENTS`
(optionally the change name) as the skill's input.
