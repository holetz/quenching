---
description: Propose a new OpenSpec change — create it and generate all artifacts in one step
argument-hint: [change-name-or-description]
---

Use the Skill tool to invoke `claude-quenching:openspec-propose`, passing `$ARGUMENTS`
(a kebab-case change name OR a description of what to build) as the skill's input.
