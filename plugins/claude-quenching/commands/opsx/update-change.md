---
description: Revise an OpenSpec change's planning artifacts and keep them coherent (never edits code)
argument-hint: [change-name]
---

Use the Skill tool to invoke `claude-quenching:openspec-update-change`, passing `$ARGUMENTS`
(optionally the change name) as the skill's input.
