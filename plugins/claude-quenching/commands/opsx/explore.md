---
description: Enter explore mode — a thinking partner with OpenSpec + OKF awareness
argument-hint: [change-name-or-topic]
---

Use the Skill tool to invoke `claude-quenching:openspec-explore`, passing `$ARGUMENTS`
(optionally a change name or the topic to explore) as the skill's input.
