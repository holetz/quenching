---
description: Sync a change's delta specs into the main specs (without archiving)
argument-hint: [change-name]
---

Use the Skill tool to invoke `claude-quenching:openspec-sync-specs`, passing `$ARGUMENTS`
(optionally the change name) as the skill's input.
