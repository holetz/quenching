---
description: Align all three fronts — docs/ then specs/ then .claude/ — on one confirmation
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-align-all`, passing `$ARGUMENTS`
(an optional scope; omit to align the whole repository) as the skill's input.
