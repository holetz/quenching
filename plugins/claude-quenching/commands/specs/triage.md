---
description: Sweep the whole front — backlog stages plus every spec's discoveries
argument-hint: [optional-slug]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-triage`, passing `$ARGUMENTS`
(optionally one slug to limit the sweep; omit to sweep everything) as the skill's input.
