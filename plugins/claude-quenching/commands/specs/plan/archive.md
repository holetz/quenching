---
description: Archive a completed plan, then offer the OKF distillation pass
argument-hint: [plan-name]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-plan-archive`, passing
`$ARGUMENTS` (optionally the plan name) as the skill's input.
