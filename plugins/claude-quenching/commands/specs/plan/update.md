---
description: Revise an plan's planning artifacts and keep them coherent (never edits code)
argument-hint: [plan-name]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-plan-update`, passing `$ARGUMENTS`
(optionally the plan name) as the skill's input.
