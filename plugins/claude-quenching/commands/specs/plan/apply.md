---
description: Implement tasks from a plan
argument-hint: [plan-name]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-plan-apply`, passing `$ARGUMENTS`
(optionally the plan name) as the skill's input.
