---
description: Close out a plan that will NOT be built — nothing to sync, and reopen its backlog task
argument-hint: [plan-name]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-plan-abandon`, passing `$ARGUMENTS`
(optionally the plan name, and the reason it is being dropped) as the skill's input.
