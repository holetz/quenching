---
description: Interrogate a plan until it is worth building — questions, one at a time, one edit
argument-hint: [plan-name] [--mode interview|critic|premortem|alternatives]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-plan-refine`, passing `$ARGUMENTS`
(optionally a plan name and/or a `--mode`) as the skill's input.
