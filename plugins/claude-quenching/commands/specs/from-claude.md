---
description: Convert a Claude Code plan file into a spec, so it can be gated and archived
argument-hint: [path-to-plan-file]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-from-claude`, passing `$ARGUMENTS`
(optionally a path to the native plan file) as the skill's input.
