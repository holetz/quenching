---
description: Close a spec out — promote to archive (done or abandoned) and distil
argument-hint: [slug] [--outcome done|abandoned]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-archive`, passing `$ARGUMENTS`
(the spec slug and optionally its outcome) as the skill's input.
