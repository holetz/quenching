---
description: Read the whole specs/ front and report where it stands — writes nothing
argument-hint: [optional-slug]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-status`, passing `$ARGUMENTS`
(optionally a spec slug to detail; omit to read the whole front) as the skill's input.
