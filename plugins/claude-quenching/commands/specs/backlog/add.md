---
description: Park ONE task in the specs/backlog inbox — fast, minimal, zero interrogation
argument-hint: [task-description]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-backlog-add`, passing `$ARGUMENTS`
(the task to capture — optionally with inline priority/tags/complexity, e.g. "park X, high
priority, theme auth, ~8h") as the skill's input.
