---
description: Triage and prioritize the WHOLE specs/backlog inbox in one plan → one OK
argument-hint: [optional-focus]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-backlog-triage`, passing `$ARGUMENTS`
(an optional focus or scope for the triage sweep; omit to triage everything) as the skill's
input.
