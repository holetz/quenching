---
description: Build a ready spec — verify, review, and commit each task
argument-hint: [slug]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-apply`, passing `$ARGUMENTS`
(optionally the spec slug) as the skill's input.
