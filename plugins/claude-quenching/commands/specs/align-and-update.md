---
description: Align AND update specs/ — align, archive, triage, looped to a fixpoint
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-specs-align-and-update`, passing `$ARGUMENTS`
(optionally a `specs/` path or a scope; omit to drive the whole `specs/` front to a fixpoint) as
the skill's input.
