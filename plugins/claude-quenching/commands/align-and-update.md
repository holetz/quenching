---
description: Align AND update every front — docs, specs, .claude — looped until nothing changes
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-align-and-update-all`, passing
`$ARGUMENTS` (an optional scope; omit to drive the whole repository to a cross-front fixpoint)
as the skill's input.
