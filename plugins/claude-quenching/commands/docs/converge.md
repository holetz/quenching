---
description: Run align → import-memory → harness → glossary-backfill to OKF convergence
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-converge`, passing `$ARGUMENTS`
(an optional scope; omit to drive the whole repo to a fixpoint) as the skill's input.
