---
description: Create or update the mkdocs site over the docs/documentation home
argument-hint: [optional-section-or-mkdocs-path]
---

Use the Skill tool to invoke `claude-quenching:quenching-docs-documentation-build`, passing
`$ARGUMENTS` (optionally a `documentation/` section to focus the nav check on, or the path of
an existing `mkdocs.yml`; omit to inventory the whole site layer) as the skill's input.
