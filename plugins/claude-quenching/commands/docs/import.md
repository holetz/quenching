---
description: Import an external source (files/folders/URLs) into the OKF bundle as many docs
argument-hint: [source-paths-or-urls]
---

Use the Skill tool to invoke `claude-quenching:quenching-docs-import`, passing `$ARGUMENTS`
(the source to ingest — local file/folder paths, or URLs) as the skill's input.
