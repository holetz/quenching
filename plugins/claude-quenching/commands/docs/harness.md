---
description: Refactor CLAUDE.md/AGENTS.md into thin pointers over the docs/ bundle
argument-hint: [optional-harness-file]
---

Use the Skill tool to invoke `claude-quenching:quenching-harness`, passing `$ARGUMENTS`
(an optional specific harness file; omit to sweep every CLAUDE.md/AGENTS.md) as the skill's
input.
