---
description: Align AND update docs/ — align, memory, harness, glossary, looped to a fixpoint
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-docs-align-and-update`, passing `$ARGUMENTS`
(an optional scope; omit to drive the whole `docs/` front to a fixpoint) as the skill's input.
