---
description: Read the whole docs/ front and report where the OKF bundle stands — writes nothing
argument-hint: [optional-home-or-path]
---

Use the Skill tool to invoke `claude-quenching:quenching-docs-status`, passing `$ARGUMENTS`
(optionally a home or path to focus; omit to read the whole bundle) as the skill's input.
