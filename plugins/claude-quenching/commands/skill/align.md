---
description: Migrate this repo's whole .claude skill/command surface to the single taxonomy
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-skill-align`, passing `$ARGUMENTS`
(an optional scope; omit to sweep the whole automation surface) as the skill's input.
