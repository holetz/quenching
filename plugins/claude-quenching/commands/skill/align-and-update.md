---
description: Align AND update .claude/ — migrate the surface, then audit every body's doctrine
argument-hint: [optional-scope]
---

Use the Skill tool to invoke `claude-quenching:quenching-skill-align-and-update`, passing
`$ARGUMENTS` (an optional scope; omit to drive the whole `.claude/` front to a fixpoint) as the
skill's input.
