# CLAUDE.md — docs/standards/

Current/active **contracts and conventions** of this repo — the detail the root
CLAUDE.md cites on demand. Organized in subject subfolders (one standard per file).

- **Where each standard lives:** [INDEX.md](INDEX.md) — the layer index (its list is
  DERIVED from disk; do not hand-edit the generated zone).
- **To create / edit / move / rename a standard here** (and keep the index + pointers
  in sync): use the docs-authoring skill (`quenching-docs`, or its installed name).
- **Boundary:** only the current/active contract lives here — direction → `vision/`,
  open decision → `decisions/`, external fact → `reference/`, our data → `catalog/`.

<!-- Auto-loaded by Claude Code when working under docs/standards/ — keep it a thin
     pointer (nearest-file navigation), never a copy of the standards themselves.
     Skeleton installed by `quenching-management`; spec in references/docs-taxonomy.md.
     ENGLISH on install: standards/ is an agent-facing surface — keep the prose,
     titles and headings English; keep structural identifiers (paths, INDEX.md,
     frontmatter keys) canonical. -->
