*ANNOUNCEMENT — <short title>*

*Scope:* `<affected area/system>`

*Status:* `<open | in progress | resolved | monitoring | informational>`

*Impact:* `<high | medium | low>`

*Audience:* `<affected areas / squads / systems>`

*Active period:* `<start — end, if resolved>`

*Action required:* `<yes — what | no — informational only>`

*Last updated:* `<YYYY-MM-DD — author>`

———

*Summary:* <one direct sentence. No technical jargon.>

*What happened:* <objective, 1-2 sentences.>
*Why it matters:* <practical impact, 1-2 sentences.>
*What to do:* <expected action, 1-2 sentences — or "nothing, informational only".>

<!-- CHANNEL TEMPLATE: chat (Slack / Teams) — quenching-management method.
     CHANNEL DIFFERENCE:
       • the header goes in the MAIN MESSAGE; the long description can go in
         a thread if preferred.
       • on EACH update, EDIT the original message (status + last-updated)
         — do NOT post a new version in thread (the original is the source).
       • PIN the message while status is `open`/`in progress`; unpin when
         `resolved`.
       • chat bold uses *single asterisk* (Slack/Teams), not **markdown**.
     Fill in the <placeholders>; the repo decides the real values. -->
