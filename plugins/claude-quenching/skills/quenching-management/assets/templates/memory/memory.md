---
name: <type>_<slug>     # type derived from the repo (e.g.: user / feedback / project / reference)
description: <1 line — hook used in relevance-based retrieval>
metadata:
  type: user | feedback | project | reference
---

<The fact, concise. For feedback/project, follow with **Why:** and **How to apply:**.
Exact dates (YYYY-MM-DD), never "recently"/"today". Do not duplicate what already
lives in the repo (code/CLAUDE.md/normative reference) — **link** via `[[other-memory]]`
or `@path`, do not copy.>

# NOTE: this is just a REFERENCE TEMPLATE. The knowledge-management method
# does NOT write memory — writing/pruning is done by the memory flow (/memory). The
# index line (title + hook + link, <~150 characters) goes in MEMORY.md.
