# `presentations/slides/`

Slide decks (`.pptx`, exports) as human deliverables. Each deck is consumed through a
**sidecar** `.md` (`type: sidecar`) that extracts the high-signal content; the LLM never
opens the binary. Starts empty; the repo fills it.
