# `presentations/diagrams/`

Diagrams as human deliverables — **prefer diagrams-as-code** (Mermaid, PlantUML, drawio
source). A binary diagram is consumed through a **sidecar** `.md` (`type: sidecar`) that
describes the flow; the LLM never opens the binary. Starts empty; the repo fills it.
