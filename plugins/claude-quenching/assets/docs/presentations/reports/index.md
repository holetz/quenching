# `presentations/reports/`

Stakeholder reports / PDFs as human deliverables. Each report is consumed through a
**sidecar** `.md` (`type: sidecar`) that extracts the thesis, decision, and key numbers;
the LLM never opens the binary. Starts empty; the repo fills it.
