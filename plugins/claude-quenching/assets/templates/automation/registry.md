---
type: documentation
title: Automation registry
description: The repo's local Claude Code automation surface — every skill and its command, derived from .claude/skills/
resource: .claude/skills/
tags: [automation, skills, commands]
timestamp: <ISO 8601 — e.g. 2026-07-20>
audience: both
authority: current
source: <who established the surface — e.g. quenching-skill-align first run>
maintainer: <owner>
---

# Automation registry

<Curated prose — one short paragraph: how this repo's automation surface is organized
(the single axis: domain-bound skills named after their folder, generic skills named
verb-object), a link to the rule at [/docs/standards/automation/skills.md](/docs/standards/automation/skills.md),
and, when relevant, a pointer to installed plugins whose commands extend the surface
(they stay out of the table below).>

The table between the markers is **DERIVED** — regenerated by
`skills.py registry reindex`, which owns its format, from the local
`.claude/skills/*/SKILL.md` frontmatter. Edit outside the markers only; a hand edit inside
them is overwritten by the next run, not merged.

<!-- GENERATED:BEGIN -->
<!-- GENERATED:END -->

<!-- MOLD (claude-quenching · automation registry) → becomes
     `docs/documentation/reference/automation.md` in a target repo's OKF bundle.

     The zone above is left EMPTY on purpose. `skills.py registry reindex` fills it from the
     surface's own SKILL.md frontmatter and owns its format — the row shape, the columns, the
     sort. A mold that also drew the table would be a second statement of that format, free to
     drift from the one that executes. Install this file, then run the tool.

     Two rules the tool cannot enforce, owned by
     `quenching-skill-new/references/taxonomy.md` §registry: the zone lists ONLY the repo's own
     surface — plugin-contributed commands (/specs:*, marketplace plugins) may be pointed at
     from the curated prose, never listed inside the markers — and the markers must exist
     before a reindex, which refuses a doc without them (`sk-no-zone`) rather than placing a
     table at a guessed anchor in prose a human wrote.

     `quenching-skill-new` (OKF tail) and `quenching-skill-align` (verify step) are the two
     skills that invoke the reindex; each ends its run on a second one reporting
     `changed: false`. STAMP = MERGE, never clobber the curated prose. -->
