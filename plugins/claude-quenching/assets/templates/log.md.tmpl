<!-- MOLD (claude-quenching · reserved `log.md` change history). Copy the body BELOW into a
     bundle's `log.md` (at `docs/` and at `docs/standards/`, and optionally per home). A
     `log.md` is an OKF reserved filename: NO concept frontmatter. Entries are grouped under
     `## YYYY-MM-DD` headings, NEWEST FIRST, each prefixed **Creation** / **Update** /
     **Deprecation** with a link to the affected concept. Do NOT copy this note. -->

# Change log — `<bundle>`

History of the bundle, most recent first. Each entry is grouped under a `## YYYY-MM-DD`
heading and prefixed `**Creation**` / `**Update**` / `**Deprecation**`.

## <YYYY-MM-DD>

**Creation**: [<Title>](/docs/<home>/<path>.md) — <one line on what/why>.
**Update**: [<Title>](/docs/<home>/<path>.md) — <what changed>.
**Deprecation**: [<Title>](/docs/<home>/<path>.md) — <superseded by / removed because>.
