---
type: idea              # OKF concept type — non-empty
title: <one-line name of the idea>
description: <one sentence — the gist of the idea>
timestamp: <ISO 8601 — e.g. 2026-07-08>
---

# <idea title>

<1–3 sentences: what the idea is, why it might matter, any seed context.
No pillar, no vision_refs, no done-criteria — that thinking happens in the
OpenSpec cycle (`openspec-explore` / `openspec-propose`), not here.>

<!-- MOLD (claude-quenching · backlog idea) → becomes `backlog/<idea-slug>.md`.
     `backlog/` is the RAW IDEA INBOX: the fast, low-ceremony landing spot for a thought
     before its scope/direction is decided, seeding the OpenSpec cycle (`openspec-explore`
     to think it through, `openspec-propose` to develop it into a change). `resource`
     is intentionally omitted — nothing is built yet to point at (the resulting `missing-resource`
     WARN is expected, not a defect). Once the change's artifacts are apply-ready
     (`openspec/changes/<name>/`), the idea file LEAVES the tree and the transition is
     recorded in `backlog/index.md`'s Developed ledger (mirroring how an implemented ADR
     distills into `standards/` and leaves `decisions/`). -->
